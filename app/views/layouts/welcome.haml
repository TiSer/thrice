!!!
%html
  %head
    %title all is flashable
    = stylesheet_link_tag    "welcome.css"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %div{:align => "left"}
      - if user_signed_in?
        You entered as
        = succeed "." do
          %b= current_user.name
        = link_to "Log out", destroy_user_session_path, :method => :delete
    = yield
